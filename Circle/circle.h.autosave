#ifndef CIRCLE_H
#define CIRCLE_H

#include <QImage>

class Circle
{
public:
	Circle( int posX, int posY, int radius );
	~Circle();
	void draw( QImage *image );
private:
	int x;
	int y;
	int r;
public slots:
	void setX( int value );
	void setY( int value );
	void setR( int value );
Q_SIGNALS:
    void valueChanged(int value);
};

#endif // CIRCLE_H
